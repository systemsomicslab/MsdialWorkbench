using System;
using System.Collections.Generic;
using System.Text;

namespace CompMs.MsdialCore.Enum {
    public enum SupportMsRawDataExtension { abf, ibf, cdf, mzml, wiff, raw, d }
    public enum MsdialDataStorageFormat { dcl, pai, arf, aef, msf, bfasta, bpep, spep }
}
