<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CompMs.Graphics.Controls">


    <Style TargetType="{x:Type local:FileOpenControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FileOpenControl}">
                    <Grid Margin="3" Background="{TemplateBinding Background}" MaxHeight="40">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="60" MinWidth="40"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:FileOpenControl}}, Path=Path}"
                                 VerticalAlignment="Center"
                                 Padding="5"
                                 Grid.Column="0" />
                        <Button Content="Browse" Margin="2,5,0,5" Name="BrowseButton" Grid.Column="1"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
