<Window x:Class="CompMs.App.Msdial.View.ExperimentSpectrum"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CompMs.App.Msdial.View"
        xmlns:chart="clr-namespace:CompMs.App.Msdial.View.Chart"
        xmlns:ccore="clr-namespace:CompMs.Graphics.Core.Base;assembly=ChartDrawing"
        xmlns:gchart="clr-namespace:CompMs.Graphics.Chart;assembly=ChartDrawing"
        xmlns:behavior="clr-namespace:CompMs.Graphics.Behavior;assembly=ChartDrawing"
        xmlns:vm="clr-namespace:CompMs.App.Msdial.ViewModel.Chart"
        xmlns:model="clr-namespace:CompMs.App.Msdial.Model.Chart"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type={x:Type vm:ExperimentSpectrumViewModel}}"
        Title="ExperimentSpectrum" Height="800" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/View/ChartStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="{x:Type gchart:MultiChart}" x:Key="SpectrumStyle">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type gchart:MultiChart}">
                            <Grid>
                                <Grid.Resources>
                                    <Style TargetType="TextBlock" x:Key="GraphTitle">
                                        <Setter Property="FontFamily" Value="Calibri"/>
                                        <Setter Property="FontSize" Value="18"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                    <Style TargetType="TextBlock" x:Key="AxisTitle">
                                        <Setter Property="FontFamily" Value="Calibri"/>
                                        <Setter Property="FontStyle" Value="Italic"/>
                                        <Setter Property="FontSize" Value="13"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </Grid.Resources>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition />
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="15"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <TextBlock Name="GraphTitle"
                                           Text="{TemplateBinding GraphTitle}"
                                           Style="{StaticResource GraphTitle}"
                                           IsHitTestVisible="False"
                                           Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"/>

                                <ScrollViewer  Grid.Row="1" Grid.Column="2">
                                    <UniformGrid Columns="1" IsItemsHost="True"/>
                                </ScrollViewer>

                                <TextBlock Name="HorizontalTitle"
                                           Text="{TemplateBinding HorizontalTitle}"
                                           Style="{StaticResource AxisTitle}"
                                           Foreground="{TemplateBinding BorderBrush}"
                                           IsHitTestVisible="False"
                                           Grid.Row="3" Grid.Column="2"/>
                                <TextBlock Name="VerticalTitle"
                                           Text="{TemplateBinding VerticalTitle}"
                                           Style="{StaticResource AxisTitle}"
                                           Foreground="{TemplateBinding BorderBrush}"
                                           IsHitTestVisible="False"
                                           Grid.Row="1" Grid.Column="0">
                                    <TextBlock.LayoutTransform>
                                        <RotateTransform Angle="-90" CenterX="0.5" CenterY="0.5"/>
                                    </TextBlock.LayoutTransform>
                                </TextBlock>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="acc" Command="{Binding AccumulateSpectrumCommand, Mode=OneTime}"/>
        </StackPanel>
        
        <chart:RangeSelectableChromatogramView DataContext="{Binding RangeSelectableChromatogramViewModel}"
                                               Grid.Row="1"/>

            <gchart:MultiChart ItemsSource="{Binding Spectrums.Value}"
                               ccore:ChartBaseControl.HorizontalAxis="{Binding HorizontalAxis}"
                               ccore:ChartBaseControl.VerticalAxis="{Binding VerticalAxis}"
                               Style="{StaticResource SpectrumStyle}"
                               Background="WhiteSmoke"
                               BorderBrush="Black"
                               Grid.Row="2">
                <gchart:MultiChart.Resources>
                    <DataTemplate DataType="{x:Type model:SpectrumCollection}">
                        <Grid Height="200">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="30"/>
                            </Grid.RowDefinitions>
                        <Border BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type gchart:MultiChart}}}"
                                Height="200"
                                BorderThickness="1,0,0,1"
                                Grid.Row="0" Grid.Column="1">
                            <Border Background="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type gchart:MultiChart}}}"
                                    behavior:ZoomByDragBehavior.IsEnabled="True"
                                    behavior:ZoomByWheelBehavior.IsEnabled="True"
                                    behavior:MoveByDragBehavior.IsEnabled="True"
                                    behavior:ResetRangeByDoubleClickBehavior.IsEnabled="True">
                                <gchart:LineSpectrumControl ItemsSource="{Binding Spectrums}"
                                                            HorizontalPropertyName="Mass"
                                                            VerticalPropertyName="Intensity"/>
                            </Border>
                        </Border>
                        <gchart:HorizontalAxisControl x:Name="PART_HorizontalAxis"
                                                     VerticalAxis="{x:Null}"
                                                     LabelBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type gchart:MultiChart}}}"
                                                     Margin="1,0,0,0"
                                                     Grid.Row="1" Grid.Column="1"/>
                        <gchart:VerticalAxisControl x:Name="PART_VerticalAxis"
                                                   HorizontalAxis="{x:Null}"
                                                   LabelBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type gchart:MultiChart}}}"
                                                   Margin="0,0,0,1"
                                                   Grid.Row="0" Grid.Column="0"/>
                            
                        </Grid>
                    </DataTemplate>
                </gchart:MultiChart.Resources>
            <gchart:MultiChart.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Save spectra as nist format"
                              Command="{Binding SaveSpectraAsNistCommand, Mode=OneTime}"/>
                </ContextMenu>
            </gchart:MultiChart.ContextMenu>
        </gchart:MultiChart>
    </Grid>
</Window>
