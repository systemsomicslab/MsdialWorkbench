<UserControl x:Class="CompMs.App.Msdial.View.BarChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompMs.App.Msdial.View"
             mc:Ignorable="d" 
             xmlns:chart="clr-namespace:CompMs.Graphics.Chart;assembly=ChartDrawing"
             xmlns:manager="clr-namespace:CompMs.Graphics.AxisManager;assembly=ChartDrawing"
             xmlns:bar="clr-namespace:CompMs.Graphics.Bar;assembly=ChartDrawing"
             Name="ClassPeakBarView"
             xmlns:vm="clr-namespace:CompMs.App.Msdial.ViewModel"
             d:DataContext="{d:DesignInstance Type={x:Type vm:BarChartViewModel}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <d:DesignerProperties.DesignStyle>
        <Style TargetType="UserControl">
            <Setter Property="Background" Value="White"/>
        </Style>
    </d:DesignerProperties.DesignStyle>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/View/ChartStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <manager:CategoryAxisManager x:Key="ClassAxis"
                                         ItemsSource="{Binding DataContext.BarItems, Source={x:Reference Name=ClassPeakBarView}}"
                                         IdentityPropertyName="Class" DisplayPropertyName="Class"/>
            <manager:AutoContinuousAxisManager x:Key="PeakAxis"
                                               ItemsSource="{Binding DataContext.BarItems, Source={x:Reference Name=ClassPeakBarView}}"
                                               ValuePropertyName="Height"
                                               Bounds="0,0"
                                               ChartMargin="0,0.25" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <chart:SimpleChartControl HorizontalAxis="{StaticResource ClassAxis}"
                                  VerticalAxis="{Binding VerticalAxis}"
                                  HorizontalTitle="Class"
                                  VerticalTitle="{Binding VerticalTitle}"
                                  Style="{StaticResource BasicChart}">
            <bar:BarControl ItemsSource="{Binding BarItems}"
                            HorizontalPropertyName="Class"
                            VerticalPropertyName="{Binding VerticalProperty}" >
                <bar:BarControl.ToolTip>
                    <ToolTip DataContext="{Binding Path=PlacementTarget, RelativeSource={RelativeSource Self}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Class: "/>
                            <TextBlock Text="{Binding FocusedItem.Class}"/>
                            <TextBlock Text=" Height: "/>
                            <TextBlock Text="{Binding FocusedItem.Height}"/>
                        </StackPanel>
                    </ToolTip>
                </bar:BarControl.ToolTip>
            </bar:BarControl>
        </chart:SimpleChartControl>
    </Grid>
</UserControl>
