<UserControl x:Class="CompMs.App.Msdial.View.Setting.ProjectSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompMs.App.Msdial.View.Setting"
             xmlns:ui="clr-namespace:CompMs.Graphics.UI;assembly=ChartDrawing"
             mc:Ignorable="d" 
             d:Background="White"
             d:DesignHeight="120" d:DesignWidth="400">
    <GroupBox Header="Start up new project"
              Padding="8">
        <StackPanel>
            <ui:LabeledBar Label="Project title:"
                           LabelWidth="96">
                <ui:LabeledBar.ToolTip>
                    Enter project title which is used as project file name.
                </ui:LabeledBar.ToolTip>
                <TextBox d:Text="ProjectTitle"
                         VerticalContentAlignment="Center"
                         Margin="8,0,0,0"/>
            </ui:LabeledBar>

            <ui:ExecutableBar Label="Project file path:"
                              LabelWidth="96"
                              ButtonContent="Browse"
                              ButtonWidth="96"
                              Margin="0,16,0,0">
                <ui:ExecutableBar.ToolTip>
                    Select a project folder which should be the same as the directory includeing abf files.
                </ui:ExecutableBar.ToolTip>
                <TextBox d:Text="/usr/project/folder/path"
                         VerticalContentAlignment="Center"
                         Margin="8,0"/>
            </ui:ExecutableBar>
        </StackPanel>
    </GroupBox>
</UserControl>
