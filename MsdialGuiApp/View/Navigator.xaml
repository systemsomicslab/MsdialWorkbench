<UserControl x:Class="CompMs.App.Msdial.View.Navigator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompMs.App.Msdial.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="150">
    <UserControl.Resources>
        <ResourceDictionary Source="/View/MainWindowStyle.xaml"/>
    </UserControl.Resources>
    <Grid Name="Grid_Navigation">

        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" MinHeight="200" />
        </Grid.RowDefinitions>

        <Label Content="File navigator" 
               Height="28"
               Foreground="Black"
               VerticalAlignment="Top"
               HorizontalContentAlignment="Center" 
               Grid.Row="0" Background="LightSteelBlue" />

        <ListBox Name="ListBox_FileName" Grid.Row="0" ItemsSource="{Binding Storage.AnalysisFiles}" BorderThickness="0" 
                 ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                 ScrollViewer.VerticalScrollBarVisibility="Auto" 
                 Margin="0,28,0,2" >
            <ToolTipService.ToolTip>
                <ToolTip Content="Double click a file to show the result in peak viewer" />
            </ToolTipService.ToolTip>
        </ListBox>

        <GridSplitter Height="2" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>

        <Label Content="Alignment navigator" Background="LightSteelBlue" 
               Height="28"
               Foreground="Black" 
               VerticalAlignment="Top"
               HorizontalContentAlignment="Center" Grid.Row="1"/>

        <ListBox Name="ListBox_AlignedFiles" Grid.Row="1" ItemsSource="{Binding Storage.AlignmentFiles}" 
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" 
                 Margin="0,28,0,0">
            <ToolTipService.ToolTip>
                <ToolTip Content="Double click a alignment file to show the result in alignment viewer" />
            </ToolTipService.ToolTip>
        </ListBox>

    </Grid>
</UserControl>
