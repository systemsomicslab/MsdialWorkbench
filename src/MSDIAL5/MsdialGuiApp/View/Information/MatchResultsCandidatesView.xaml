<UserControl x:Class="CompMs.App.Msdial.View.Information.MatchResultsCandidatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:result="clr-namespace:CompMs.Common.DataObj.Result;assembly=Common"
             mc:Ignorable="d" 
             d:DesignHeight="240" d:DesignWidth="360">
    <ListBox ItemsSource="{Binding Candidates.Value}"
             SelectedItem="{Binding SelectedCandidate.Value, Mode=TwoWay}">
        <ListBox.ItemTemplate>
            <DataTemplate DataType="{x:Type result:MsScanMatchResult}">
                <StackPanel>
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0}({1})">
                                <Binding Path="Name" Mode="OneTime"/>
                                <Binding Path="TotalScore" StringFormat="F3" Mode="OneTime"/>
                            </MultiBinding> 
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock Text="{Binding AnnotatorID, StringFormat=Annotation method: {0}, Mode=OneTime}" />
                </StackPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
